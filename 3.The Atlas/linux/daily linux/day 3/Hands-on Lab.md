Run these commands:

```
free -h
```

See how much RAM and swap your system is using.

```
cat /proc/meminfo
```

Explore detailed memory information exposed by the kernel.

```
vmstat 1 5
```

Watch memory and CPU statistics update every second for five seconds.